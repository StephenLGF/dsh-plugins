/** Package-owned invariant companion for the DSH PR assistant plugin. */
const PACKAGE_NAME = '@stephen1620/dsh-pr-assistant'

export const name = 'pr-assistant-invariant'

export function apply(): void {
  void PACKAGE_NAME
}
